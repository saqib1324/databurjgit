<h1>editing</h1>

<%= form_for(@section) do |f| %>
  <% if @section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@section.errors.count, "error") %> prohibited this instructor from being saved:</h2>

      <ul>
      <% @section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>

      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Section Name" %><br>
    <%= f.text_field :section_name %>
  </div>
  <div class="field">
    <%= f.label "Room Location" %><br>
    <%= f.text_field :room_location %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
